<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Floating companion: draw over other apps, stay resident, show a notice -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
      Voice input for the companion. The microphone itself is owned by the
      system recognition service, not by us; this permission is what lets that
      service accept our requests. Requested at runtime, and only useful when
      the user switches voice input on.
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.PocketLLM">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Keeps the bubble alive after the user leaves the app. specialUse fits
          an always-available companion; the ongoing notification is what makes
          the process (and therefore the loaded model) survive.
        -->
        <service
            android:name=".companion.CompanionOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="floating_companion_overlay" />
        </service>

        <!--
          Read-only page access for the "Summarize" action. It is opt-in, never
          dispatches gestures, and is only queried on an explicit tap.
        -->
        <service
            android:name=".companion.CompanionAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/companion_accessibility_config" />
        </service>

        <!--
          Reminder delivery. Declared rather than registered at runtime so a
          reminder still arrives when the app process is not running.
        -->
        <receiver
            android:name=".companion.ReminderReceiver"
            android:exported="false" />
    </application>

</manifest>
